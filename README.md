#   git init
#   git config --global user.name "им€"
#   git config --global user.email "почта"
#   git add .
#   git commit -m "подпись"
#   git remote add origin https://github.com(свой удаленный репозиторий)
#   git branch -M main
#   git push -u origin main


#   Publish repository     Repository -> Repository Settings -> Remote




















